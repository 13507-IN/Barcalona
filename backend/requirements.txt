# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0

# Web Scraping
requests==2.31.0
beautifulsoup4==4.12.2

# Optional extras (may require build tools on Windows)
# lxml==4.9.3
# aiohttp==3.8.5

# Optional: For better WSGI server (use this in production instead of Flask dev server)
Werkzeug==2.3.7

# Optional: For environment variables
python-dotenv==1.0.0
